version: "2"
sql:
  - engine: "postgresql"
    queries: ".db/sql/query/"
    schema: ".db/sql/migrations/"
    gen:
      go:
        package: "db"
        out: "./db"
        emit_json_tags: true
        emit_prepared_queries: true